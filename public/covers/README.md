# Product covers

Drop cover images here (e.g. `steam-500.png`) and set the product's image URL
in the admin catalog (`/admin/ns-gifts`) to the matching path, e.g.
`/covers/steam-500.png`.

Products without a cover fall back to a templated brand-coloured card
(see `components/ProductCover.tsx`).
